---
armv6:
  DEB_ARCH: "armhf"
  PACKAGING_DEBIAN_VERSION: "${PACKAGING_DEBIAN_VERSION}"
  PACKAGING_PYTHON_VERSION: "${PACKAGING_PYTHON_VERSION}"
  PIP_INDEX_URL: "https://piwheels.org/simple"
  PLATFORM: "linux/arm/v7"
armv7:
  DEB_ARCH: "armhf"
  PACKAGING_DEBIAN_VERSION: "${PACKAGING_DEBIAN_VERSION}"
  PACKAGING_PYTHON_VERSION: "${PACKAGING_PYTHON_VERSION}"
  PIP_INDEX_URL: "https://piwheels.org/simple"
  PLATFORM: "linux/arm/v7"
arm64:
  DEB_ARCH: "arm64"
  PACKAGING_DEBIAN_VERSION: "${PACKAGING_DEBIAN_VERSION}"
  PACKAGING_PYTHON_VERSION: "${PACKAGING_PYTHON_VERSION}"
  PIP_INDEX_URL: "https://pypi.org/simple"
  PLATFORM: "linux/arm64"
